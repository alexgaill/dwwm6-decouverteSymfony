{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form) }}
        {{form_label(form.title, "Catégorie")}}
        {{form_widget( form.title, {"attr": {"class": "form-test", "value":"coucou"}} ) }}
    {{ form_end(form) }}
{% endblock %}